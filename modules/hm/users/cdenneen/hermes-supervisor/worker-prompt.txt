Execute exactly one bounded AXIS Development Supervisor cycle using the attached skill and preflight JSON. Use only the supplied deterministic inventory and execution graph. Enforce this canonical repository ownership boundary without fallback: supervisor orchestration/temporary Slack/cron -> cdenneen/home; AXIS runtime/product -> ghostspace/axis; Contracts/PlanningRecords -> ghostspace/axis-governance; deployment/realistic validation -> ghostspace/axis-lab. Run the exact `cycle_command` from preflight once; it owns dispatch, lease acquisition, Prompt Factory context, and Hermes-native worker launch. Review the returned assignment/result against current GitLab/repository evidence. Never edit inventory, graph, decomposition, lease, assignment, control, skill, cron, schema, contract, or credential files directly. Never long-poll a non-terminal pipeline. Return only the local audit handoff format from the skill; do not send Slack messages.
