This directory is for OpenCode local configuration.

- `.opencode/agents/` contains repo-scoped agents (tracked).
- Use the `nixos-expert` agent for NixOS / nix-darwin / Home Manager changes in this repo.
- `node_modules/`, `package.json`, and `bun.lock` are intentionally ignored.
